/* Snell, Dallin
** 01 JUN 2019
** This will dynamically change the columns and row in the textareas.
** This isn't ideal (because it should be in CSS), but the CSS properties don't do what I want.
*/


$("textarea").attr("cols", "35").attr("rows", "10");
